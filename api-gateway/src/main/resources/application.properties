# API Gateway Configuration
spring.application.name=api-gateway
server.port=8081

# Eureka Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Gateway Configuration
spring.cloud.gateway.routes[0].id=room-sensors-service
spring.cloud.gateway.routes[0].uri=lb://room-sensors-service
spring.cloud.gateway.routes[0].predicates[0]=Path=/api/sensors/**
spring.cloud.gateway.routes[0].predicates[1]=Path=/api/measurements/**
spring.cloud.gateway.routes[0].predicates[2]=Path=/api/auth/**
spring.cloud.gateway.routes[0].predicates[3]=Path=/api/users/**

# CORS Configuration
spring.cloud.gateway.globalcors.cors-configurations.[/].allowed-origins=*
spring.cloud.gateway.globalcors.cors-configurations.[/].allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.cloud.gateway.globalcors.cors-configurations.[/].allowed-headers=*

# Logging
logging.level.org.springframework.cloud.gateway=DEBUG
